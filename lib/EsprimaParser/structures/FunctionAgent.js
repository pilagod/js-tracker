class FunctionAgent extends Function {
  // @TODO: set prototype constructor to itself
  // body, params, prototype (constructor: default this), parser, scriptUrl, closureStack
  // superClass
}

module.exports = FunctionAgent
